# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Cuckoo+OCMock-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :ios, '9.0'

  # Pods for Cuckoo+OCMock-iOS
  pod 'OCMock'

  target 'Cuckoo+OCMock_iOSTests' do

  end
end

target 'Cuckoo-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :ios, '9.0'

  # Pods for Cuckoo-iOS

end

target 'Cuckoo-macOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  platform :osx, '10.10'

  # Pods for Cuckoo-macOS

end
